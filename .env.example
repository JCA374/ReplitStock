# Stock Analysis Application - Environment Variables
# Copy this file to .env and fill in your actual values
# The application works WITHOUT any API keys using cached data and Yahoo Finance

# =============================================================================
# ALPHA VANTAGE API (OPTIONAL)
# =============================================================================
# Used for: Premium stock data and fundamentals
# Sign up: https://www.alphavantage.co/support/#api-key (FREE)
# Fallback: Yahoo Finance (automatic, no key needed)
#
# Status: OPTIONAL - App works without this
ALPHA_VANTAGE_API_KEY=

# =============================================================================
# SUPABASE CLOUD DATABASE (OPTIONAL)
# =============================================================================
# Used for: Cloud sync across devices, team collaboration
# Sign up: https://supabase.com/ (FREE tier available)
# Fallback: SQLite local database (automatic)
#
# Status: OPTIONAL - App works with local SQLite database
SUPABASE_URL=
SUPABASE_KEY=

# =============================================================================
# DEPLOYMENT MODES
# =============================================================================
# The application has 3 deployment modes:
#
# 1. FULL MODE (All API keys provided)
#    - Real-time stock data from Alpha Vantage
#    - Cloud sync via Supabase
#    - Best for production use
#
# 2. HYBRID MODE (Some API keys)
#    - Mix of cloud and local storage
#    - Either Alpha Vantage OR Supabase
#    - Good for personal use
#
# 3. OFFLINE MODE (No API keys)
#    - Uses Yahoo Finance (free, no key needed)
#    - Local SQLite database
#    - Perfect for testing and development
#    - FULLY FUNCTIONAL with cached data
#
# =============================================================================
# PERFORMANCE TUNING (OPTIONAL)
# =============================================================================
# Override bulk scanner settings for your deployment environment
# Leave commented to use defaults from config.py

# BULK_MAX_API_WORKERS=3
# BULK_API_BATCH_SIZE=50
# BULK_ANALYSIS_BATCH_SIZE=50
# BULK_MAX_ANALYSIS_WORKERS=12

# =============================================================================
# SECURITY NOTES
# =============================================================================
# - NEVER commit the .env file to version control
# - The .env file is already in .gitignore
# - For Streamlit Cloud, use the Secrets management feature instead
# - Rotate your API keys periodically
# - Use the 'anon' key for Supabase (not service_role in production)
